{{#each erros}}
<div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}


<h3>Editar Categoria</h3>
<div class="card">
  <div class="card-body">
    <form action="/main/categories/edit" method="POST">
      <input type="hidden" value="{{category._id}}">
      <label for="nome">Nome: </label>
        <input type="text" id="nome" name="nome" class="form-control" value="{{category.nome}}">
      <label for="link">Slug: </label>
        <input type="text" id="slug" name="slug" class="form-control" value="{{category.slug}}">
        <hr>
        <button type="submit" class="btn btn-success">Editar Categoria</button>
    </form>
    
  </div>
</div>
